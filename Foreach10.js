// Crear un array nuevo con todos los valores multiplicados por 3, [1, 4, 7]

let numeros = [1, 4, 7];
let multiplicados = [];

numeros.forEach((valor) => {
  multiplicados.push(valor * 3);
});
console.log(multiplicados);
