// Recorrer un string letra por letra hacia atrás

// Ejemplo: si let palabra = "agustin";
// Tenés que imprimir las letras desde el final hasta el inicio.

let str = "agustin";

for (let index = 6; index >= 0; index--) {
  console.log(str[index]);
}
