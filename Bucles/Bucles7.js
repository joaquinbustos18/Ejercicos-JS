// Recorré desde 1 hasta 100 y usá un if para imprimir solo los que sean múltiplos de 3.
// (n % 3 === 0)

for (let index = 1; index <= 100; index++) {
  if (index % 3 === 0) {
    console.log(index);
  }
}
